#[cfg(test)]
mod dir_test {
    use brave_utils::fs::gen_symlink_default_skin;
    use std::io::Error;

    #[test]
    fn link_test1() {
        // 目标目录路径和软链接路径
        // gen_symlink_default_skin("hello".to_string().as_str())
        // println!("{}",);
    }
}
